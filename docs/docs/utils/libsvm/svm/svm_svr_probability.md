Mojo function

# `svm_svr_probability`

```mojo
fn svm_svr_probability[k_t: Int](prob: svm_problem, param: svm_parameter) -> Float64
```

**Parameters:**

- **k_t** (`Int`)

**Args:**

- **prob** (`svm_problem`)
- **param** (`svm_parameter`)

**Returns:**

`Float64`

