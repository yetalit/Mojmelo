Mojo function

# `svm_svr_probability`

```mojo
fn svm_svr_probability(prob: svm_problem, param: svm_parameter) -> Float64
```

**Args:**

- **prob** (`svm_problem`)
- **param** (`svm_parameter`)

**Returns:**

`Float64`

