Mojo function

# `dot`

```mojo
fn dot(var px: UnsafePointer[svm_node, origin_of(MutOrigin.external)], var py: UnsafePointer[svm_node, origin_of(MutOrigin.external)]) -> Float64
```

**Args:**

- **px** (`UnsafePointer`)
- **py** (`UnsafePointer`)

**Returns:**

`Float64`

