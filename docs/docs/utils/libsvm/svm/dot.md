Mojo function

# `dot`

```mojo
fn dot(var px: UnsafePointer[svm_node], var py: UnsafePointer[svm_node]) -> Float64
```

**Args:**

- **px** (`UnsafePointer`)
- **py** (`UnsafePointer`)

**Returns:**

`Float64`

