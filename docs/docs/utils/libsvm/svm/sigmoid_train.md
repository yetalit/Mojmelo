Mojo function

# `sigmoid_train`

```mojo
def sigmoid_train(l: Int, dec_values: UnsafePointer[Float64, MutExternalOrigin], labels: UnsafePointer[Float64, MutExternalOrigin], mut A: Float64, mut B: Float64)
```

**Args:**

- **l** (`Int`)
- **dec_values** (`UnsafePointer`)
- **labels** (`UnsafePointer`)
- **A** (`Float64`)
- **B** (`Float64`)

