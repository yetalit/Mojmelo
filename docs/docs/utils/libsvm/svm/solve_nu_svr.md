Mojo function

# `solve_nu_svr`

```mojo
def solve_nu_svr(prob: svm_problem, param: svm_parameter, alpha: UnsafePointer[Float64, MutExternalOrigin], mut si: SolutionInfo)
```

**Args:**

- **prob** (`svm_problem`)
- **param** (`svm_parameter`)
- **alpha** (`UnsafePointer`)
- **si** (`SolutionInfo`)

