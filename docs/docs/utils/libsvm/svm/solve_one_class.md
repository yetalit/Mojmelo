Mojo function

# `solve_one_class`

```mojo
fn def solve_one_class(prob: svm_problem, param: svm_parameter, alpha: UnsafePointer[Float64, MutUntrackedOrigin], mut si: SolutionInfo)
```

**Args:**

- **prob** (`svm_problem`)
- **param** (`svm_parameter`)
- **alpha** (`UnsafePointer[Float64, MutUntrackedOrigin]`)
- **si** (`SolutionInfo`)

