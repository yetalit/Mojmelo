Mojo function

# `solve_one_class`

```mojo
fn solve_one_class(prob: svm_problem, param: svm_parameter, alpha: UnsafePointer[Float64, origin_of(MutOrigin.external)], mut si: SolutionInfo)
```

**Args:**

- **prob** (`svm_problem`)
- **param** (`svm_parameter`)
- **alpha** (`UnsafePointer`)
- **si** (`SolutionInfo`)

