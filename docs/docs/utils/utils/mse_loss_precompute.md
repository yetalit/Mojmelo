Mojo function

# `mse_loss_precompute`

```mojo
fn mse_loss_precompute(size: Float32, sum: Float32, sum_sq: Float32) -> Float32
```

**Args:**

- **size** (`Float32`)
- **sum** (`Float32`)
- **sum_sq** (`Float32`)

**Returns:**

`Float32`

**Raises:**

