Mojo function

# `mse_loss`

```mojo
def mse_loss(y: Matrix, weights: Matrix, size: Float32) -> Float32
```

**Args:**

- **y** (`Matrix`)
- **weights** (`Matrix`)
- **size** (`Float32`)

**Returns:**

`Float32`

**Raises:**

