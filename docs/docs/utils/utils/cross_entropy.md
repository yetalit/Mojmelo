Mojo function

# `cross_entropy`

```mojo
def cross_entropy(y: Matrix, y_pred: Matrix) -> Float32
```

Binary Cross Entropy.

**Args:**

- **y** (`Matrix`)
- **y_pred** (`Matrix`)

**Returns:**

`Float32`: The loss.

**Raises:**

