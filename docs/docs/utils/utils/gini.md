Mojo function

# `gini`

```mojo
fn gini(y: Matrix, weights: Matrix, size: Float32) -> Float32
```

**Args:**

- **y** (`Matrix`)
- **weights** (`Matrix`)
- **size** (`Float32`)

**Returns:**

`Float32`

**Raises:**

