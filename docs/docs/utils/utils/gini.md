Mojo function

# `gini`

```mojo
def gini(y: Matrix, weights: Matrix, size: Float32) -> Float32
```

**Args:**

- **y** (`Matrix`)
- **weights** (`Matrix`)
- **size** (`Float32`)

**Returns:**

`Float32`

**Raises:**

