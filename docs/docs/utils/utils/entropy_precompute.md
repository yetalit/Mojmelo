Mojo function

# `entropy_precompute`

```mojo
fn entropy_precompute(size: SIMD[float32, 1], histogram: List[Int]) -> SIMD[float32, 1]
```

**Args:**

- **size** (`SIMD`)
- **histogram** (`List`)

**Returns:**

`SIMD`

**Raises:**

