Mojo function

# `gini_precompute`

```mojo
def gini_precompute(size: Float32, histogram: List[Int]) -> Float32
```

**Args:**

- **size** (`Float32`)
- **histogram** (`List`)

**Returns:**

`Float32`

**Raises:**

