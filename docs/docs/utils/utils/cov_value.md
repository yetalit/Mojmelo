Mojo function

# `cov_value`

```mojo
fn cov_value(x_mean_diff: Matrix, y_mean_diff: Matrix) -> Float32
```

**Args:**

- **x_mean_diff** (`Matrix`)
- **y_mean_diff** (`Matrix`)

**Returns:**

`Float32`

**Raises:**

