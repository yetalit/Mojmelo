Mojo function

# `sub`

```mojo
fn sub[dtype: DType, width: Int](a: SIMD[dtype, width], b: SIMD[dtype, width]) -> SIMD[dtype, width]
```

**Parameters:**

- **dtype** (`DType`)
- **width** (`Int`)

**Args:**

- **a** (`SIMD[dtype, width]`)
- **b** (`SIMD[dtype, width]`)

**Returns:**

`SIMD[dtype, width]`

