Mojo function

# `leaf_loss`

```mojo
fn leaf_loss(reg_lambda: Float32, reg_alpha: Float32, g: Matrix, h: Matrix) -> Float32
```

**Args:**

- **reg_lambda** (`Float32`)
- **reg_alpha** (`Float32`)
- **g** (`Matrix`)
- **h** (`Matrix`)

**Returns:**

`Float32`

**Raises:**

