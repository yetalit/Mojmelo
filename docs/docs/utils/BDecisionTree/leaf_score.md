Mojo function

# `leaf_score`

```mojo
fn leaf_score(reg_lambda: Float32, reg_alpha: Float32, g: Matrix, h: Matrix) -> Float32
```

**Args:**

- **reg_lambda** (`Float32`)
- **reg_alpha** (`Float32`)
- **g** (`Matrix`)
- **h** (`Matrix`)

**Returns:**

`Float32`

**Raises:**

