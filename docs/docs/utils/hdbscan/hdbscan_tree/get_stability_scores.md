Mojo function

# `get_stability_scores`

```mojo
def get_stability_scores(mut labels: List[Scalar[DType.int]], clusters: Set[Scalar[DType.int]], stability: Dict[Scalar[DType.int], Float32], max_lambda: Float32) -> List[Float32]
```

**Args:**

- **labels** (`List`)
- **clusters** (`Set`)
- **stability** (`Dict`)
- **max_lambda** (`Float32`)

**Returns:**

`List`

**Raises:**

