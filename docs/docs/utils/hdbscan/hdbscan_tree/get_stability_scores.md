Mojo function

# `get_stability_scores`

```mojo
fn get_stability_scores(mut labels: List[Scalar[DType.index]], clusters: Set[Scalar[DType.index]], stability: Dict[Scalar[DType.index], Float32], max_lambda: Float32) -> List[Float32]
```

**Args:**

- **labels** (`List`)
- **clusters** (`Set`)
- **stability** (`Dict`)
- **max_lambda** (`Float32`)

**Returns:**

`List`

**Raises:**

