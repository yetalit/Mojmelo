Mojo function

# `get_probabilities`

```mojo
fn get_probabilities(tree: Dict[String, List[Int]], lambda_array: List[Float32], cluster_map: Dict[Int, Int], labels: List[Int], deaths: List[Float32]) -> List[Float32]
```

**Args:**

- **tree** (`Dict[String, List[Int]]`)
- **lambda_array** (`List[Float32]`)
- **cluster_map** (`Dict[Int, Int]`)
- **labels** (`List[Int]`)
- **deaths** (`List[Float32]`)

**Returns:**

`List[Float32]`

**Raises:**

