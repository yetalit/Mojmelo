Mojo function

# `key`

```mojo
fn def key(idx: Int, data: Pointer[Float32, MutUntrackedOrigin], dim: Int, split_dim: Int) -> Float32
```

**Args:**

- **idx** (`Int`)
- **data** (`Pointer[Float32, MutUntrackedOrigin]`)
- **dim** (`Int`)
- **split_dim** (`Int`)

**Returns:**

`Float32`

