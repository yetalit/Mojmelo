Mojo function

# `key`

```mojo
fn key(idx: Scalar[DType.index], data: UnsafePointer[Float32, MutAnyOrigin], dim: Int, split_dim: Int) -> Float32
```

**Args:**

- **idx** (`Scalar`)
- **data** (`UnsafePointer`)
- **dim** (`Int`)
- **split_dim** (`Int`)

**Returns:**

`Float32`

