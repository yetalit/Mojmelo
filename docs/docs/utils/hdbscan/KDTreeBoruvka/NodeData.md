Mojo struct

# `NodeData`

```mojo
@memory_only
struct NodeData
```

## Fields

- **is_leaf** (`Bool`)
- **idx_start** (`Int`)
- **idx_end** (`Int`)
- **radius** (`Float32`)
- **center** (`CenterPtr`)

## Implemented traits

`AnyType`, `Copyable`, `ImplicitlyDestructible`, `Movable`

