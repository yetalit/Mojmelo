Mojo function

# `upperbidiagonalization_unblocked`

```mojo
fn def upperbidiagonalization_unblocked(mut mat: Mat, mut diag: Vec, mut superdiag: Vec)
```

**Args:**

- **mat** (`Mat`)
- **diag** (`Vec`)
- **superdiag** (`Vec`)

