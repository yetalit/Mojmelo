Mojo function

# `vec_sub_scaled_inplace`

```mojo
fn def vec_sub_scaled_inplace(mut y: Vec, z: Vec, var scale: Float64)
```

Y -= scale * Z (elementwise, same length).

**Args:**

- **y** (`Vec`)
- **z** (`Vec`)
- **scale** (`Float64`)

