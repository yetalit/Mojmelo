Mojo function

# `upperbidiagonalization_inplace_blocked`

```mojo
fn def upperbidiagonalization_inplace_blocked(mut A: Mat, mut diag: Vec, mut superdiag: Vec, max_block_size: Int = Int(16))
```

**Args:**

- **A** (`Mat`)
- **diag** (`Vec`)
- **superdiag** (`Vec`)
- **max_block_size** (`Int`)

