Mojo function

# `vec_add_scaled_inplace`

```mojo
fn def vec_add_scaled_inplace(mut y: Vec, z: Vec, scale: Float64)
```

Y += scale * Z.

**Args:**

- **y** (`Vec`)
- **z** (`Vec`)
- **scale** (`Float64`)

