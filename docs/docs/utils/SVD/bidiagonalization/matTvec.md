Mojo function

# `matTvec`

```mojo
fn def matTvec(A: Mat, x: Vec) -> Vec
```

Y = A^T * X. y has length A.cols(), x must have length A.rows().

**Args:**

- **A** (`Mat`)
- **x** (`Vec`)

**Returns:**

`Vec`

