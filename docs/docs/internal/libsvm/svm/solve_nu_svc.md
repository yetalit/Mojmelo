Mojo function

# `solve_nu_svc`

```mojo
fn def solve_nu_svc(prob: svm_problem, param: svm_parameter, alpha: Pointer[Float64, MutUntrackedOrigin], mut si: SolutionInfo)
```

**Args:**

- **prob** (`svm_problem`)
- **param** (`svm_parameter`)
- **alpha** (`Pointer[Float64, MutUntrackedOrigin]`)
- **si** (`SolutionInfo`)

