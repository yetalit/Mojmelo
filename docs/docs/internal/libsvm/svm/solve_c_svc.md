Mojo function

# `solve_c_svc`

```mojo
fn def solve_c_svc(prob: svm_problem, param: svm_parameter, alpha: Pointer[Float64, MutUntrackedOrigin], mut si: SolutionInfo, Cp: Float64, Cn: Float64)
```

**Args:**

- **prob** (`svm_problem`)
- **param** (`svm_parameter`)
- **alpha** (`Pointer[Float64, MutUntrackedOrigin]`)
- **si** (`SolutionInfo`)
- **Cp** (`Float64`)
- **Cn** (`Float64`)

