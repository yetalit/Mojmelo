Mojo function

# `finish_jacobi_svd`

```mojo
fn def finish_jacobi_svd(mut work: Mat, mut U_out: Mat, mut V_out: Mat, diag_size: Int, maxCoeff: Float64, mut S_out: Vec) -> ComputationInfo
```

**Args:**

- **work** (`Mat`)
- **U_out** (`Mat`)
- **V_out** (`Mat`)
- **diag_size** (`Int`)
- **maxCoeff** (`Float64`)
- **S_out** (`Vec`)

**Returns:**

`ComputationInfo`

