Mojo function

# `apply_rotation_cols_direct`

```mojo
fn def apply_rotation_cols_direct(mut M: Mat, p: Int, q: Int, rot: JacobiRotation)
```

**Args:**

- **M** (`Mat`)
- **p** (`Int`)
- **q** (`Int`)
- **rot** (`JacobiRotation`)

