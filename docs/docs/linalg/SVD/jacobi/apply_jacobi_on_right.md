Mojo function

# `apply_jacobi_on_right`

```mojo
fn def apply_jacobi_on_right(mut m: Mat, row_start: Int, row_count: Int, col_p: Int, col_q: Int, rot: JacobiRotation)
```

**Args:**

- **m** (`Mat`)
- **row_start** (`Int`)
- **row_count** (`Int`)
- **col_p** (`Int`)
- **col_q** (`Int`)
- **rot** (`JacobiRotation`)

