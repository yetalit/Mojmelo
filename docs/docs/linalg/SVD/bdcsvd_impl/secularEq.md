Mojo function

# `secularEq`

```mojo
fn def secularEq(mu: Float64, col0: Vec, diag: Vec, perm: IVec, diagShifted: Vec, shift: Float64) -> RealScalar
```

**Args:**

- **mu** (`Float64`)
- **col0** (`Vec`)
- **diag** (`Vec`)
- **perm** (`IVec`)
- **diagShifted** (`Vec`)
- **shift** (`Float64`)

**Returns:**

`RealScalar`

