Mojo function

# `eq`

```mojo
fn def eq[dtype: DType, width: Int](a: SIMD[dtype, width], b: SIMD[dtype, width]) -> SIMD[.bool, width]
```

**Parameters:**

- **dtype** (`DType`)
- **width** (`Int`)

**Args:**

- **a** (`SIMD[dtype, width]`)
- **b** (`SIMD[dtype, width]`)

**Returns:**

`SIMD[.bool, width]`

